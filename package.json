{
  "name": "airfoil-visualizer",
  "version": "1.0.0",
  "scripts": {
    "start": "node backend/server.js",
    "postinstall": "cd backend && npm install && cd ../frontend && npm install && npx vite build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1"
  }
}
